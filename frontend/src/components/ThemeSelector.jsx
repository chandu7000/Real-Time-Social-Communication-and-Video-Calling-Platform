import { CheckIcon, PaletteIcon } from "lucide-react";
import { THEMES } from "../constants";
import { useThemeStore } from "../store/useThemeStore";
const ThemeSelector=()=>{const {theme,setTheme}=useThemeStore();return <div className="dropdown dropdown-end"><button type="button" tabIndex={0} className="btn btn-ghost btn-circle" aria-label="Choose appearance theme" title="Choose theme"><PaletteIcon className="size-5" /></button><div tabIndex={0} className="dropdown-content z-[60] mt-3 max-h-[420px] w-64 overflow-y-auto rounded-[1.5rem] border border-base-300/80 bg-base-100/95 p-2.5 shadow-2xl backdrop-blur-xl"><div className="flex items-center gap-2 px-3 pb-3 pt-2"><span className="grid size-8 place-items-center rounded-xl bg-primary/10 text-primary"><PaletteIcon className="size-4"/></span><div><p className="text-xs font-black uppercase tracking-[.16em] opacity-55">Appearance</p><p className="text-[11px] opacity-45">Choose your Zenvio mood</p></div></div><div className="space-y-1">{THEMES.map((o)=><button type="button" key={o.name} className={`flex w-full items-center gap-3 rounded-xl px-3 py-2.5 text-left transition focus:outline-none focus-visible:ring-2 focus-visible:ring-primary ${theme===o.name?"bg-primary/10 text-primary":"hover:bg-base-200"}`} onClick={()=>setTheme(o.name)} aria-pressed={theme===o.name}><PaletteIcon className="size-4"/><span className="text-sm font-bold">{o.label}</span><span className="ml-auto flex items-center gap-1">{o.colors.map(c=><span key={c} className="size-2.5 rounded-full ring-1 ring-base-content/10" style={{backgroundColor:c}}/>)}{theme===o.name&&<CheckIcon className="ml-1 size-4"/>}</span></button>)}</div></div></div>};
export default ThemeSelector;
