{
  "name": "real-time-social-communication-video-calling-platform",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Real-Time Social Communication and Video Calling Platform",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
